<section class="main-section row">
  <div class="column column--third column--left">
    <h1>Articles</h1>
  </div>
  <div class="column column--two-thirds column--right column--no-padding">
    <h2>There’s just something about writing ideas down that make you think differently. Communicating ideas effectively is also a big part of design. I need to get better at this, but here are a couple of things I’ve been thinking about lately.</h2>
  </div>
</section>

<% @articles.group_by {|a| a.date.year }.sort.reverse_each do |year, articles| %>
  <section class="main-section row">
    <div class="column column--third column--left">
      <h2><%= year %></h2>
    </div>
    <ul class="article-list column column--two-thirds column--right column--no-padding">
      <% articles.each do |article| %>
        <li class="article-list__item">
          <a href="<%= article.url %>">
            <h4><%= article.title %></h4>
            <p><%= article.excerpt %></p>
          </a>
        </li>
      <% end %>
    </ul>
  </section>
<% end %>
